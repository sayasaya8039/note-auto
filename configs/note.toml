# note RSS 単独リサーチ用設定
# 用途: note 内の人気記事から 1 記事生成

[trends]
sources = ["note"]
window_hours = 24
max_per_source = 30

[scoring.source_weights]
note = 2.0

[schedule]
daily_top = 1

# note 自動投稿 / X 告知だけスキップ。Slack 進捗通知は受け取る。
[publish]
note_publish = false
x_announce = false
